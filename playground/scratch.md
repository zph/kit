``` playground
require "./src/pk.cr"
# puts system("ls -lah #{dir.to_s}/")
# system("cp -a #{dir.to_s}/teleport .")
# LOG.debug("output"){ [stdout.to_s, stderr.to_s, process] }
# binaries.to_a.map do |bin|
#   LOG.debug("glob") { Dir.glob("#{dir}/**/*") }
#   glob_match = Dir.glob("#{dir}/**/{#{bin}}").find { |m| File.file?(m) }
#   LOG.info("glob") { glob_match }
#
#   if glob_match
#     copy(glob_match, outputname, bin)
#   end
# end
```
